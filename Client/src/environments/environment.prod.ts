// Production environment configuration for the Angular app.
// In this project, the production API URL currently matches the local backend.
export const environment = {
  production: true,
  apiUrl: 'http://localhost:4003/api/agent',
  toolsUrl: 'http://localhost:4003/api/tools'
};
